package dev.steampad.config;

/**
 * Shared pixel-art color theme for SteamPad's custom UI (virtual keyboard, radial menu) — Minecraft
 * material palettes the user cycles through in Ajustes, applied consistently across every screen
 * that supports theming instead of each one inventing its own color set.
 */
public enum PixelTheme { VANILLA, OAK, STONE, EMERALD, REDSTONE, LAPIS, AMETHYST, NETHER }
