# Gradle
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true

# Fabric — targeting 1.21.10 runtime (text-render methods changed int→void in 1.21.10;
# building against the real runtime version is the only way to get binary-compatible call sites)
minecraft_version=1.21.10
yarn_mappings=1.21.10+build.3
loader_version=0.16.14
fabric_version=0.138.4+1.21.10

# SteamPad Mod
mod_version=0.61.0
maven_group=dev.steampad
archives_base_name=steampad

# Dependencies
steamworks4j_version=1.9.0
cloth_config_version=20.0.149
modmenu_version=16.0.0
