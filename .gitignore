# Gradle / Java build outputs
.gradle/
build/
out/
bin/
# Final mod jars — build output, one per variant per version. Never commit these: there are already
# ~110 of them here (~300 MB) and they are regenerated by `build`.
dist/

# Minecraft Run directory (logs, saves, run cache)
run/
logs/

# IDE files
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project

# System & OS temp files
.DS_Store
Thumbs.db
*.log
*.log.gz

# Local Graphify outputs (re-generated dynamically)
graphify-out/
